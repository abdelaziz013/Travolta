import React from 'react';
import SearchBar from '../components/SearchBar/SearchBar';

const Hotel = () => {
    return (
        <div>
            <SearchBar />
        </div>
    );
}

export default Hotel;